// data.js
// 这是你的博客数据库，每次写新文章，就复制一个花括号 {} 添加在最前面

const blogPosts = [
       	{
        id: 4,
        title: "HTML基础了解及指南",
        category: "HTML基础",
        date: "2026-04-19",
        content: `
## 第一部分：理解核心概念

HTML 并不是一门编程语言，而是一门标记语言。它通过给文本“打标签”来告诉浏览器这些内容是什么（比如：这是一个标题，那是一张图片）。

- **标签 (Tags)**： HTML 的基本语法单位，通常成对出现，包裹在尖括号中，例如 \`<p>\`。
- **元素 (Elements)**： 包含开始标签、内容和结束标签的完整结构，例如 \`<p>这是一段文字</p>\`。
- **属性 (Attributes)**： 写在开始标签里，提供关于元素的额外信息。例如 \`<a href="https://example.com">\` 中的 \`href\` 就是属性。

## 第二部分：标准网页的“骨架”

任何一个标准的 HTML 页面都必须包含以下基础骨架。你可以新建一个 \`.html\` 文件，将这段代码复制进去并在浏览器中打开。

\`\`\`html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的第一个网页</title>
</head>
<body>
    <h1>欢迎来到我的网站</h1>
</body>
</html>
\`\`\`

- \`<!DOCTYPE html>\`：声明文档类型，告诉浏览器这是现代的 HTML5 文档。
- \`<html>\`：整个页面的根元素。
- \`<head>\`：网页的“大脑”，包含不在页面上直接显示的元数据（如字符集、网页标题、引入的样式表等）。
- \`<body>\`：网页的“身体”，包含用户可见的所有视觉内容。

## 第三部分：常用基础标签

掌握以下这些最常用的标签，你就可以构建出图文并茂的网页了。

- **标题标签 (\`<h1>\` 到 \`<h6>\`)**： 用于定义重要性递减的标题。一个页面通常只有一个 \`<h1>\`。
- **段落标签 (\`<p>\`)**： 用于定义一段普通的文本内容。
- **超链接标签 (\`<a>\`)**： 用于页面跳转。\`href\` 属性指定目标地址。例如：\`<a href="about.html">关于我</a>\`。
- **图像标签 (\`<img>\`)**： 用于嵌入图片。这是一个单标签，不需要结束标签。必须包含 \`src\`（图片路径）和 \`alt\`（图片无法加载时的替代文本）属性。
- **列表标签 (\`<ul>\`, \`<ol>\`, \`<li>\`)**： \`<ul>\` 定义无序列表（项目符号），\`<ol>\` 定义有序列表（数字序号），\`<li>\` 定义具体的列表项。

## 第四部分：构建现代模块化布局 (实战教学)

现代网页设计非常注重“模块化”和“语义化”。这意味着我们不再只是用一堆无意义的 \`<div>\` 来拼凑页面，而是使用具有明确含义的标签（如 \`<header>\`, \`<main>\`, \`<aside>\`, \`<section>\`, \`<article>\`）来构建清晰的结构。

这种清晰的 HTML 结构，能够为你后续编写复杂的 CSS 样式（比如悬浮弹出效果、透明卡片、或者全屏背景）打下完美的底层基础。

让我们来看一个现代个人博客的典型 HTML 结构示例：

\`\`\`html
<body>
  <div class="video-bg-container">
    <video autoplay loop muted playsinline>
      <source src="background.mp4" type="video/mp4">
    </video>
  </div>

  <header>
    <h1>我的设计笔记</h1>
  </header>

  <div class="layout-wrapper">
    
    <aside class="sidebar">
      <section class="profile-card">
        <img src="avatar.jpg" alt="我的头像" class="avatar">
        <div class="profile-info">
          <h2>昵称</h2>
          <p>前端与 UI/UX 设计爱好者</p>
        </div>
      </section>

      <section class="tags-card">
        <h3>内容标签</h3>
        <a href="#"># 设计思考</a>
        <a href="#"># CSS 技巧</a>
      </section>
    </aside>

    <main class="content-area">
      <article class="post-card">
        <h2>如何实现优雅的半透明边框</h2>
        <p>在这篇文章中，我们将探讨现代 UI 设计中的透明度运用...</p>
        <a href="#" class="read-more">阅读全文</a>
      </article>
    </main>

  </div>
</body>
\`\`\`

**结构解析：**
- 我们使用 \`<video>\` 标签搭建了一个视频背景的底层。
- \`<aside>\` 明确定义了侧边栏区域。
- 我们在侧边栏中使用了两个独立的 \`<section>\`。这样可以确保人物资料区和标签区是完全分开的独立卡片，互不干扰，方便后续单独为它们设计不同的交互效果。
- \`<main>\` 标记了页面的核心内容，里面包裹着具体的 \`<article>\`（文章卡片）。

## 第五部分：学习建议与最佳实践

1. **保持嵌套清晰**： HTML 标签一层套一层，一定要注意缩进对齐，这样代码才具有可读性。
2. **拥抱语义化**： 尽量少用毫无意义的 \`<div>\` 和 \`<span>\`，多使用能表达内容含义的标签，这不仅对搜索引擎友好，也让代码结构一目了然。
3. **为 CSS 留好“钩子”**： 在写 HTML 时，养成给重要的元素加上 class（类名）的习惯（比如上面例子中的 \`class="profile-card"\`）。这些类名就是未来用 CSS 进行精确样式定位的控制点。
        `
    }, 
	 {
        id: 5,
        title: "HTML基础标签",
        category: "HTML基础",
	summary: "这是一篇关于HTML基础标签的终极指南...",
        date: "2026-04-19",
        content: `
## 基础标签
### 1. 标题标签 (\`<h1> - <h6>\`)
标题标签用于定义网页内容的层级结构。数字越小，字体默认越大，层级也越高。
- \`<h1>\`：最核心的标题。一个页面通常只应该有一个 \`<h1>\`，它代表了整个页面的核心主题（对搜索引擎优化 SEO 极其重要）。
- \`<h2> - <h6>\`：副标题。依次往下细分内容的层级。
### 2. 段落与文本区块
- \`<p>\`：段落 (Paragraph)。用于包裹一段完整的文字。浏览器会自动在段落前后添加间距，让文本更容易阅读。
- \`<br>\`：换行 (Break)。这是一个“单标签”（不需要结束标签 \`</br>\`）。在 \`<p>\` 内部如果需要强制换行，可以使用它。
- \`<hr>\`：水平线 (Horizontal Rule)。也是单标签，用于在视觉上分隔不同的内容区块。
### 3. 链接与图像 (连接世界的桥梁)
- \`<a>\`：锚点/链接 (Anchor)。这是网页能互相跳转的关键。
	- 核心属性 href：告诉浏览器链接要跳去哪里（可以是外部网址、内部页面，甚至是页面内的某个位置）。
	- 常用属性 target="_blank"：如果你希望链接在一个新的浏览器标签页中打开，加上这个属性即可。
	- 示例：<a href="https://www.google.com" target="_blank">访问 Google</a>
	\`<a href="https://www.google.com" target="_blank">访问 Google</a>\`
- \`<img>\`：图像 (Image)。用于在页面上显示图片（单标签）。
	- 核心属性 src：图片的来源路径（网络地址或本地相对路径）。
	- 核心属性 alt：替代文本。当图片因为网络原因无法加载时，会显示这段文字；同时它对视障用户使用的屏幕阅读器非常关键，是不可省略的好习惯。
	- 示例：<img src="my-dog.jpg" alt="一只金毛在草地上奔跑">
  	\`<img src="my-dog.jpg" alt="一只金毛在草地上奔跑">\`
### 4. 列表 (整理信息的利器)
- \`<ul>\`：无序列表 (Unordered List)。默认使用小圆点作为项目符号，适合没有特定顺序的内容（如：购物清单、文章标签）。
- \`<ol>\`：有序列表 (Ordered List)。默认使用数字 (1, 2, 3...) 作为序号，适合有步骤或排名的内容（如：操作教程、排行榜）。
- \`<li>\`：列表项 (List Item)。无论是有序还是无序列表，里面的每一项都必须用 \`<li>\` 包裹。
`
	},

     {
        id: 3,
        title: "Markdown 核心语法",
        category: "Markdown",
        date: "2026-04-19",
        content: `
# Markdown 语法整理

## 1. 标题 (Headings)
使用 \`#\` 号加上一个空格，再输入文字即可表示标题。\`#\` 的数量代表标题的层级（最多支持六级）。

# # 这是一个一级标题 (最大)
## ## 这是一个二级标题
### ### 这是一个三级标题
###### ###### 这是一个六级标题 (最小)

## 2. 文本格式 (Emphasis)
通过星号 \`*\` 或下划线 \`_\` 包裹文本来实现加粗和斜体：

* **加粗**：使用 \`**文字**\` ➡️ **这是加粗的文字**
* *斜体*：使用 \`*文字*\` ➡️ *这是斜体的文字*
* ***加粗且斜体***：使用 \`***文字***\` ➡️ ***这是加粗且斜体的文字***
* ~~删除线~~：使用 \`~~文字~~\` ➡️ ~~这是带有删除线的文字~~

## 3. 列表 (Lists)
**无序列表**：使用 \`-\`、\`+\` 或 \`*\` 加上一个空格。
- 苹果
- 香蕉
- 橘子

**有序列表**：使用数字加上英文句号 \`.\` 和一个空格。
1. 打开冰箱
2. 把大象放进去
3. 关上冰箱

## 4. 链接与图片 (Links & Images)
它们的格式非常相似，区别在于图片前面多了一个英文感叹号 \`!\`。

**超链接：**
\`[点击访问百度](https://www.baidu.com)\` ➡️ [点击访问百度](https://www.baidu.com)

**插入图片：**
\`![这是一张小狗的图片](图片网址或路径.jpg)\`
*(注：方括号内是图片加载失败时的替代文本，圆括号内是图片的本地路径或网络网址。)*

## 5. 引用 (Blockquotes)
当你想引用名人名言或强调某段内容时，使用 \`>\` 加上一个空格。
语法示范：\> Stay hungry, stay foolish. —— Steve Jobs
> Stay hungry, stay foolish. —— Steve Jobs

## 6. 代码 (Code)
**行内代码**：用反引号包裹（键盘左上角 Esc 键下面的那个键）。
语法示范：请使用 \\\`Ctrl + C\\\` 复制这段内容。
实际效果：请使用 \`Ctrl + C\` 复制这段内容。

**代码块**：用三个连续的反引号&#96;&#96;&#96;包裹多行代码。你还可以在开头的反引号后面指定编程语言名称（如 html, javascript），以获得语法高亮。

\`\`\`html
<div>
    <p>这是一段 HTML 代码</p>
</div>
\`\`\`

## 7. 分割线 (Horizontal Rules)
连续使用三个或以上的星号 \`***\`、减号 \`---\` 或底线 \`___\` 即可生成一条分割线。

---

以防在最后看不清分割线
        `
    },
   {
        id: 2,
        title: "老弟刘子豪",
        category: "日常",
        date: "2026-04-19",
        content: `my brother<br>
        
先看看照片<br><br> <img src='my-brother/my-brother1.jpg'> <br>又干净、又真诚、还阳光，简直太招人喜欢了！！！
<br> <img src='my-brother/my-brother2.jpg'> <br>
<br> <img src='my-brother/my-brother3.jpg'> <br>PS：左一为另一个老弟
<br> <img src='my-brother/my-brother4.jpg'> <br>
<br> <img src='my-brother/my-brother5.jpg'> <br>
<br> <img src='my-brother/my-brother6.jpg'> <br>
给他全网征集对象`
    },

   {
        id: 1,
        title: "第一次尝试在GitHub部署博客",
        category: "日常",
        date: "2026-04-15",
        content: `今天把博客的后端去掉了，改成了纯静态页面。
        
虽然不能在网页上直接打字发布了，但把数据写在代码里感觉更极客了！
这种方式也完美解决了权限问题，因为除了我，没人能动我的 GitHub 仓库。`
    },
      
];